<UserControl x:Class="NHM.Wpf.Views.Help.HelpItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NHM.Wpf.Views.Help"
             xmlns:nhmMining="clr-namespace:NHMCore.Notifications;assembly=NHMCore"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=nhmMining:Notification}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="itemErrorTitle" Text="{Binding Name}" Grid.Row="0" Style="{StaticResource FontsTypography.SmallRegularTextBlock}"/>
        <TextBlock x:Name="itemErrorContent" Text="{Binding NotificationContent}" Grid.Row="1" Style="{StaticResource FontsTypography.SmallRegularTextBlock}"/>
        <Button Content="REMOVE" Grid.Row="2"  Click="RemoveNotification" />
        <Button x:Name="ActionButton" Content="Action" Grid.Row="3"  Click="ExecuteNotificationAction" />
    </Grid>
</UserControl>
