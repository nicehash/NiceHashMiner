<UserControl x:Class="NHM.Wpf.Views.Help.HelpItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NHM.Wpf.Views.Help"
             xmlns:viewModelsHelp="clr-namespace:NHM.Wpf.ViewModels.Help"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewModelsHelp:NotificationsElementVM}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="itemErrorTitle" Text="{Binding NotificationName}" Grid.Row="0" Style="{StaticResource FontsTypography.SmallRegularTextBlock}"/>
        <TextBlock x:Name="itemErrorContent" Text="{Binding NotificationContent}" Grid.Row="1" Style="{StaticResource FontsTypography.SmallRegularTextBlock}"/>
    </Grid>
</UserControl>
