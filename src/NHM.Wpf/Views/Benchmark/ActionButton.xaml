<UserControl x:Class="NHM.Wpf.Views.Benchmark.ActionButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NHM.Wpf.Views.Benchmark"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="50">
    <Grid Background="Transparent">
        <Border BorderBrush="#eeeeee" BorderThickness="2" CornerRadius="4" Background="White">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="1"/>
            </Border.Effect>
        </Border>
        <Border BorderBrush="Transparent" BorderThickness="0" HorizontalAlignment="Center">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"> 
                <Border Background="{StaticResource Gray1ColorBrush}" Width="5" Height="5" ></Border>
                <Border Background="{StaticResource Gray1ColorBrush}" Width="5" Height="5" Margin="0,4,0,0"></Border>
                <Border Background="{StaticResource Gray1ColorBrush}" Width="5" Height="5" Margin="0,4,0,0"></Border>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
